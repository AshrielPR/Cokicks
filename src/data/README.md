# CoKicks Catalog Data

Los listings publicos se leen desde Supabase. No se incluyen productos de
ejemplo ni fotos stock en el proyecto.

Las fotos reales se suben desde el panel privado `/admin` y quedan en el bucket
`product-images`. Un listing solo aparece al cliente cuando esta marcado como
visible en catalogo.
